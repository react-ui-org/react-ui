import React from 'react';
import type { FormLayoutCustomFieldContextValue } from './FormLayoutCustomField.types';

export const FormLayoutCustomFieldContext = React.createContext<FormLayoutCustomFieldContextValue | null>(null);
